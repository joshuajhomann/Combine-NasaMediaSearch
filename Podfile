# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NasaImageBrowser' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NasaImageBrowser

  target 'NasaImageBrowserTests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'Kingfisher', '~> 5.0'
  pod 'Tags','~> 0.3.0'

end
